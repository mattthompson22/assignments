function isEvenOrOdd(num) {
    //receive # as argument
    //if number is even, return 'even'
    //if num is odd, return odd
    if (num % 2 === 0)
      return "even";
    } else {
    //   (num % 2 !== 0)
    return "odd";
    }